name: rename
version: '1.0'
purpose: Rename a section to a new name
url: ''
interface: rename_section.py
attribution: ["CosmoSIS Team"]
rules: You can do what you want with this file.
cite: []
assumptions: []

explanation: "Use this module to rename one section to a new name.  This is only needed in quite complex pipelines"
params:
    source:
        meaning: Comma or space-separated list of source section names
        type: str
        default:
    dest:
        meaning: Comma or space-separated list of destination section names, matching one-to-one with source
        type: str
        default:

inputs: {}
outputs: {}
